{% block body %}
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <div>
                <h1 class="nav-item">EcoIT</h1>
            </div>
            <button class="navbar-toggler"
                    type="button"
                    data-bs-toggle="collapse"
                    data-bs-target="#navbarNavTest"
                    aria-controls="navbarNavTest"
                    aria-expanded="false"
                    aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarNavtest">
                <ul class="navbar-nav">
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('app_teacher_register') }}">Devenir Instructeur</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('app_student_formation') }}">Devenir Apprenant</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('app_login') }}">Se connecter</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="example-wrapper">
        {% if app.user %}
            <div class="mb-2">
                <a href="{{ path('app_logout') }}">Logout</a>
            </div>
        {% endif %}

    </div>
{% endblock %}